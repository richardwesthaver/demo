#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"			;
|#

;; clean:;rm -rf out *.fasl;pushd rust;cargo clean;popd

(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '() :silent t)
  )

(defpackage :ros.script.clean.3891893753
  (:use :cl))
(in-package :ros.script.clean.3891893753)

(defun main (&rest argv)
  (declare (ignorable argv))
  (write-line "> cargo clean")
  (uiop:run-program "cargo clean")
  (write-line "  cleaned rust crates"))
;;; vim: set ft=lisp lisp:
