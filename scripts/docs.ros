#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '() :silent t))

(defun main (&rest argv)
  (declare (ignorable argv))
  (write-line "> cargo doc")
  (uiop:run-program "cargo doc")
  (write-line "  generated rust documentation"))
