#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '(demo) :silent t))

(defun main (&rest argv)
  (declare (ignorable argv))
  (demo:main))
