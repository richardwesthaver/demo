#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '() :silent t))

(defun main (&rest argv)
  (declare (ignorable argv))
  (write-line "> cargo test")
  (uiop:run-program "cargo test")
  (write-line "  tested rust crates"))
