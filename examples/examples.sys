(defsys :examples
 :require (:user)
 :components 
 ((:file "vegadat")
  (:file "mbdump")
  (:dir "db")
  (:dir "net")
  (:dir "cli")
  (:mod "xlib"
   :components
   ((:file "menu")
    (:file "bezier")
    (:file "beziertest" :require ("bezier"))
    (:file "clclock")
    (:file "clipboard")
    (:file "xlib-demos")
    (:file "gl-test")
    ;; FIXME: compiling this generates 30-odd spurious code
    ;; deletion notes.  Find out why, and either fix or
    ;; workaround the problem.
    (:file "mandel")
    (:file "zoid")
    (:file "image")
    (:file "trapezoid" :require ("zoid"))))))
